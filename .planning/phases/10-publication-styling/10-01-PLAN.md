---
phase: 10-publication-styling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - R/efficacy_table.R
  - tests/testthat/test-efficacy_table.R
  - man/efficacy_table.Rd
autonomous: true

must_haves:
  truths:
    - "User can pass font_family to efficacy_table() and the rendered HTML contains that font family name"
    - "User can pass font_size to efficacy_table() and the rendered HTML reflects the specified pixel size"
    - "User can pass row_padding to efficacy_table() and the rendered HTML reflects the specified pixel padding"
    - "Calling efficacy_table() without new parameters produces identical output to current behavior"
  artifacts:
    - path: "R/efficacy_table.R"
      provides: "efficacy_table with font_family, font_size, row_padding parameters"
      contains: "opt_table_font"
    - path: "tests/testthat/test-efficacy_table.R"
      provides: "Tests for new styling parameters"
      contains: "font_family"
    - path: "man/efficacy_table.Rd"
      provides: "Updated roxygen docs with new parameter documentation"
      contains: "font_family"
  key_links:
    - from: "R/efficacy_table.R"
      to: "gt::opt_table_font"
      via: "conditional application when font_family is non-NULL"
      pattern: "opt_table_font.*font_family"
    - from: "R/efficacy_table.R"
      to: "gt::tab_options"
      via: "conditional application for font_size and row_padding"
      pattern: "tab_options.*table\\.font\\.size"
---

<objective>
Add publication styling parameters (font_family, font_size, row_padding) to efficacy_table() for STYLE-01 and STYLE-02 requirements.

Purpose: Users producing regulatory tables need controlled typography and spacing for publication without post-hoc gt piping. These parameters expose gt's existing styling APIs as convenient function arguments.
Output: Updated efficacy_table() with three new optional parameters, tests, and roxygen documentation.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-publication-styling/10-RESEARCH.md

@R/efficacy_table.R
@tests/testthat/test-efficacy_table.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add font_family, font_size, and row_padding parameters to efficacy_table()</name>
  <files>R/efficacy_table.R, man/efficacy_table.Rd</files>
  <action>
Add three new parameters to efficacy_table() function signature, all defaulting to NULL:
- `font_family = NULL` (character string, e.g. "Times New Roman")
- `font_size = NULL` (numeric, in pixels, e.g. 10)
- `row_padding = NULL` (numeric, in pixels, e.g. 3)

Place them after `pval_threshold` and before `...` in the signature.

Add roxygen @param documentation for each:
- font_family: "Optional character string specifying the font family for the table. When NULL (default), uses gt's default font. Applied via gt::opt_table_font()."
- font_size: "Optional numeric value specifying the table font size in pixels. When NULL (default), uses gt's default size. Applied via gt::tab_options()."
- row_padding: "Optional numeric value specifying the vertical padding for data rows in pixels. When NULL (default), uses gt's default padding. Smaller values (e.g., 2-3) create compact regulatory-style tables."

At the END of the function body (after all existing gt table construction, just before the final `tbl` return statement on line 228), add conditional styling blocks:

```r
# --- Publication styling (optional) ---
if (!is.null(font_family)) {
  tbl <- gt::opt_table_font(tbl, font = font_family)
}
if (!is.null(font_size)) {
  tbl <- gt::tab_options(tbl, table.font.size = gt::px(font_size))
}
if (!is.null(row_padding)) {
  tbl <- gt::tab_options(tbl, data_row.padding = gt::px(row_padding))
}
```

IMPORTANT: Accept numeric values for font_size and row_padding, wrap with gt::px() internally. Do NOT accept character strings like "10px" -- the function provides the simpler numeric API.

IMPORTANT: Do NOT validate font availability. Invalid font names silently fall back to defaults in gt -- this is standard behavior.

After editing R/efficacy_table.R, run `Rscript -e "devtools::document()"` to regenerate the .Rd file.
  </action>
  <verify>
Run: `Rscript -e "devtools::document()"` -- should complete without errors.
Run: `Rscript -e "devtools::load_all(); tbl <- efficacy_table(rbmiUtils:::make_test_pool()); print(class(tbl))"` -- should print "gt_tbl" confirming no breakage.
Verify man/efficacy_table.Rd contains font_family, font_size, row_padding param entries.
  </verify>
  <done>efficacy_table() accepts font_family, font_size, and row_padding parameters. NULL defaults preserve current behavior. Non-NULL values apply gt styling. Roxygen docs updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add tests for efficacy_table styling parameters</name>
  <files>tests/testthat/test-efficacy_table.R</files>
  <action>
Add the following test blocks to the end of tests/testthat/test-efficacy_table.R:

1. **font_family test**: Create mock pool, call `efficacy_table(mock_pool, font_family = "Courier")`, render with `gt::as_raw_html(tbl)`, assert `grepl("Courier", html, fixed = TRUE)` is TRUE. This works because gt::opt_table_font() injects the font name into inline CSS.

2. **font_size test**: Call `efficacy_table(mock_pool, font_size = 10)`, render HTML, assert `grepl("10px", html, fixed = TRUE)` is TRUE. The gt::tab_options with table.font.size injects the pixel value.

3. **row_padding test**: Call `efficacy_table(mock_pool, row_padding = 2)`, render HTML, assert `grepl("2px", html, fixed = TRUE)` is TRUE. The gt::tab_options with data_row.padding injects the pixel value.

4. **NULL defaults backward compatibility test**: Call `efficacy_table(mock_pool)` (no new params), render HTML, assert it does NOT contain "Courier" (confirming default font is not overridden). This ensures NULL defaults truly do nothing.

5. **Combined styling test**: Call `efficacy_table(mock_pool, font_family = "Georgia", font_size = 11, row_padding = 3)`, assert returns gt_tbl class and renders without error.

All tests should use `skip_if_not_installed("gt")` at the top. Use the existing `make_mock_pool()` helper already defined in the test file.

Run the full test suite to confirm all tests pass: `Rscript -e "devtools::test(filter = 'efficacy_table')"`.
  </action>
  <verify>
Run: `Rscript -e "devtools::test(filter = 'efficacy_table')"` -- all tests pass, including new styling tests.
Run: `Rscript -e "devtools::check()"` -- no new NOTEs, WARNINGs, or ERRORs (or at least no regressions from current baseline).
  </verify>
  <done>5 new tests verify font_family, font_size, row_padding parameters individually, combined, and NULL-default backward compatibility. All existing tests continue to pass.</done>
</task>

</tasks>

<verification>
1. `Rscript -e "devtools::test(filter = 'efficacy_table')"` -- all tests pass
2. `Rscript -e "devtools::check()"` -- no regressions
3. Confirm efficacy_table() without new params produces same output as before (NULL defaults)
4. Confirm efficacy_table(pool_obj, font_family = "Courier", font_size = 10, row_padding = 2) renders with specified styling in HTML
</verification>

<success_criteria>
- STYLE-01 satisfied: font_family parameter works, gt HTML contains specified font
- STYLE-02 satisfied: font_size and row_padding parameters work, gt HTML contains specified px values
- Backward compatible: NULL defaults produce identical output to current behavior
- All tests pass (existing + new)
- R CMD check clean (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/10-publication-styling/10-01-SUMMARY.md`
</output>
