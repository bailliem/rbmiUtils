---
phase: 07-site-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - _pkgdown.yml
autonomous: false

must_haves:
  truths:
    - "pkgdown navbar shows Get Started link, Articles dropdown, News link, GitHub link, and light/dark toggle"
    - "Function reference page groups all 22 functions + 4 S3 methods + 2 datasets into 9 named sections by layer"
    - "Sharing the site URL on social media shows an open graph card with the rbmiUtils logo and package description"
    - "Site footer displays openpharma and pharmaverse links"
  artifacts:
    - path: "_pkgdown.yml"
      provides: "Complete pkgdown site configuration"
      contains: "navbar"
    - path: "_pkgdown.yml"
      provides: "Reference grouping"
      contains: "reference"
    - path: "_pkgdown.yml"
      provides: "Open graph metadata"
      contains: "opengraph"
    - path: "_pkgdown.yml"
      provides: "Custom footer"
      contains: "footer"
  key_links:
    - from: "_pkgdown.yml navbar"
      to: "articles/pipeline.html"
      via: "intro component href"
      pattern: "articles/pipeline\\.html"
    - from: "_pkgdown.yml reference"
      to: "NAMESPACE exports"
      via: "explicit function listing"
      pattern: "validate_data|analyse_mi_data|tidy_pool_obj|efficacy_table"
    - from: "_pkgdown.yml opengraph"
      to: "man/figures/logo.png"
      via: "image src path"
      pattern: "man/figures/logo\\.png"
    - from: "_pkgdown.yml footer"
      to: "openpharma.github.io"
      via: "markdown link in community component"
      pattern: "openpharma"
---

<objective>
Configure the complete `_pkgdown.yml` with navbar, reference grouping, open graph metadata, and custom footer.

Purpose: This transforms the minimal pkgdown config into a professional, navigable site. Covers SITE-02 (navbar), SITE-03 (reference groups), SITE-04 (open graph cards), and SITE-05 (footer). SITE-01 (logo) was handled in Plan 01.

Output: Fully configured `_pkgdown.yml` covering all 5 SITE requirements.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-site-polish/07-RESEARCH.md
@.planning/phases/07-site-polish/07-01-SUMMARY.md
@_pkgdown.yml
@NAMESPACE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete _pkgdown.yml configuration</name>
  <files>_pkgdown.yml</files>
  <action>
Replace the existing minimal `_pkgdown.yml` (currently just URL + bootstrap 5) with the complete configuration below. This is the exact YAML to write -- use it verbatim, do not modify the structure:

```yaml
url: https://openpharma.github.io/rbmiUtils/

template:
  bootstrap: 5
  opengraph:
    image:
      src: man/figures/logo.png
      alt: "rbmiUtils hex logo - Utilities and Extensions for rbmi"
    twitter:
      card: summary

home:
  title: "rbmiUtils: Utility Functions to Support and Extend rbmi"
  description: >
    Bridges rbmi analysis results into publication-ready regulatory tables
    and forest plots for clinical trial workflows.

navbar:
  structure:
    left: [intro, reference, articles, news]
    right: [search, github, lightswitch]
  components:
    intro:
      text: Get Started
      href: articles/pipeline.html
    articles:
      text: Articles
      menu:
        - text: "End-to-End Pipeline"
        - text: "From rbmi Analysis to Regulatory Tables"
          href: articles/pipeline.html
        - text: "-------"
        - text: "Workflow Guides"
        - text: "Storing and Analyzing Imputed Data"
          href: articles/analyse2.html
        - text: "Data Preparation and Validation"
          href: articles/data-preparation.html
        - text: "Efficient Storage of Imputed Data"
          href: articles/efficient-storage.html

reference:
  - title: "Data Preparation"
    desc: "Validate and prepare data before imputation"
    contents:
      - validate_data
      - prepare_data_ice
      - summarise_missingness
  - title: "Analysis"
    desc: "Apply analysis functions across imputed datasets"
    contents:
      - analyse_mi_data
      - gcomp_responder
      - gcomp_responder_multi
      - gcomp_binary
  - title: "Tidying"
    desc: "Tidy and extract results from pooled objects"
    contents:
      - tidy_pool_obj
      - extract_trt_effects
      - extract_lsm
  - title: "Reporting"
    desc: "Publication-ready tables and plots"
    contents:
      - efficacy_table
      - plot_forest
  - title: "Formatting"
    desc: "Format values for publication"
    contents:
      - format_pvalue
      - format_estimate
      - format_results_table
      - format_results
  - title: "Storage"
    desc: "Efficient storage of imputed datasets"
    contents:
      - reduce_imputed_data
      - expand_imputed_data
  - title: "Utilities"
    desc: "Helper functions for imputed data workflows"
    contents:
      - get_imputed_data
      - create_impid
      - combine_results
      - pool_to_ard
  - title: "Print & Summary Methods"
    desc: "S3 methods for inspecting rbmiUtils objects"
    contents:
      - starts_with("print.")
      - starts_with("summary.")
  - title: "Datasets"
    desc: "Example datasets for demonstrations"
    contents:
      - ADEFF
      - ADMI

footer:
  structure:
    left: developed_by
    right: [built_with, community]
  components:
    community: >
      Part of the [openpharma](https://openpharma.github.io/) ecosystem
      | [pharmaverse](https://pharmaverse.org/)
```

**Critical checks after writing:**
1. Count all functions in reference section: must be exactly 22 exports + 4 S3 methods (via starts_with) + 2 datasets = 28 topics. Cross-reference against NAMESPACE.
2. Verify all 4 vignette filenames match actual files in `vignettes/`: pipeline.Rmd, analyse2.Rmd, data-preparation.Rmd, efficient-storage.Rmd.
3. Ensure YAML is valid (no tabs, consistent indentation with 2 spaces).

**What to avoid:**
- Do NOT use `has_concept()` selectors -- the package does not use @family or @concept roxygen tags.
- Do NOT add `as_analysis2`, `as_simple_formula2`, or `extract_covariates2` -- they are NOT exported (not in NAMESPACE).
- Do NOT omit the `lightswitch` from navbar right -- it enables dark/light mode toggle.
  </action>
  <verify>
- `grep -c "contents:" _pkgdown.yml` should show 9 (one per reference group)
- `grep "opengraph" _pkgdown.yml` confirms open graph section exists
- `grep "navbar" _pkgdown.yml` confirms navbar section exists
- `grep "footer" _pkgdown.yml` confirms footer section exists
- `grep "pharmaverse" _pkgdown.yml` confirms pharmaverse link in footer
- Run in R: `pkgdown::build_reference_index()` -- should complete without "Topics missing from index" warnings
  </verify>
  <done>
- _pkgdown.yml contains navbar, reference, opengraph, and footer sections
- All 22 exported functions + 4 S3 methods + 2 datasets assigned to reference groups
- Navbar has Get Started, Reference, Articles, News on left; Search, GitHub, lightswitch on right
- Footer has openpharma and pharmaverse links
- Open graph configured with logo.png image
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify pkgdown site builds correctly</name>
  <files>docs/index.html</files>
  <action>
Build the complete pkgdown site by running `pkgdown::build_site()` in R, then present the built site for human verification. The site should be built into the `docs/` directory. After building, open `docs/index.html` to verify the configuration is working.
  </action>
  <verify>
Human verifies the following in the built site:
1. Logo appears in the top-left navbar area
2. Navbar shows: "Get Started" | "Reference" | "Articles" dropdown | "News" on left
3. Navbar shows: search box | GitHub icon | light/dark toggle on right
4. "Get Started" links to the pipeline vignette
5. "Articles" dropdown shows section headers and all 4 vignettes
6. "Reference" page shows 9 groups (Data Preparation, Analysis, Tidying, Reporting, Formatting, Storage, Utilities, Print and Summary Methods, Datasets)
7. Footer shows "Part of the openpharma ecosystem | pharmaverse" on the right side
8. Browser tab shows favicon
  </verify>
  <done>
- User has confirmed the pkgdown site displays correctly with all 5 SITE requirements
- Or user has described specific issues that need fixing
  </done>
</task>

</tasks>

<verification>
1. `_pkgdown.yml` has all 5 SITE-0x requirements configured
2. `pkgdown::build_reference_index()` reports no missing topics
3. `pkgdown::build_site()` completes without errors
4. Built site displays logo, navbar, grouped reference, footer
5. Open graph meta tags present in docs/index.html
</verification>

<success_criteria>
- Navbar has Get Started, Reference, Articles, News, GitHub, lightswitch
- Reference page shows 9 function groups covering all 28 topics
- Open graph meta tags include logo image and package description
- Footer shows openpharma and pharmaverse links
- Site builds without warnings about missing reference topics
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-polish/07-02-SUMMARY.md`
</output>
