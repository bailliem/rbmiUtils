---
phase: 07-site-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - man/figures/logo.png
  - README.Rmd
  - README.md
  - pkgdown/favicon
autonomous: true

must_haves:
  truths:
    - "man/figures/logo.png exists and is a valid PNG image"
    - "README displays the logo using the standard logo.png path"
    - "Favicons are generated and committed in pkgdown/favicon/"
  artifacts:
    - path: "man/figures/logo.png"
      provides: "Standard pkgdown logo file for auto-detection"
      contains: "PNG image data"
    - path: "pkgdown/favicon/favicon.ico"
      provides: "Browser tab favicon"
    - path: "README.Rmd"
      provides: "README source with logo.png reference"
      contains: "man/figures/logo.png"
    - path: "README.md"
      provides: "Rendered README with logo.png reference"
      contains: "man/figures/logo.png"
  key_links:
    - from: "README.Rmd"
      to: "man/figures/logo.png"
      via: "HTML img tag"
      pattern: "man/figures/logo\\.png"
    - from: "pkgdown/favicon"
      to: "man/figures/logo.png"
      via: "pkgdown::build_favicons() reads logo.png"
      pattern: "favicon"
---

<objective>
Rename the hex logo to pkgdown's standard path, update README references, and generate favicons.

Purpose: pkgdown auto-detects `man/figures/logo.png` for navbar display, favicon generation, and Open Graph images. The current file is named `rbmiUtils.png` which breaks auto-detection. This plan establishes the logo foundation that SITE-01, SITE-04, and the navbar depend on.

Output: `man/figures/logo.png`, updated README.Rmd/README.md, and `pkgdown/favicon/` directory with generated favicons.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-site-polish/07-RESEARCH.md
@_pkgdown.yml
@README.Rmd
@NAMESPACE
@man/figures/rbmiUtils.png
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename logo and update README references</name>
  <files>man/figures/logo.png, README.Rmd, README.md</files>
  <action>
1. Copy `man/figures/rbmiUtils.png` to `man/figures/logo.png` (keep the original to avoid breaking any external links).

2. In `README.Rmd`, update the logo reference from `man/figures/rbmiUtils.png` to `man/figures/logo.png`. The line to change is:
   ```
   # rbmiUtils <a href="https://openpharma.github.io/rbmiUtils/"> <img src="man/figures/rbmiUtils.png" align="right" width="140px" alt="rbmiUtils website" /> </a>
   ```
   Change to:
   ```
   # rbmiUtils <a href="https://openpharma.github.io/rbmiUtils/"> <img src="man/figures/logo.png" align="right" width="140px" alt="rbmiUtils website" /> </a>
   ```

3. Render README.md from README.Rmd. Use R to run:
   ```r
   rmarkdown::render("README.Rmd", output_format = "github_document", quiet = TRUE)
   ```
   If rendering fails due to package dependencies (rbmi MCMC), instead do a simple text replacement in README.md: change `man/figures/rbmiUtils.png` to `man/figures/logo.png`. The README.md was previously generated and the only change needed is the image path.

Note: Do NOT delete `man/figures/rbmiUtils.png` -- keep it to avoid breaking any external references.
  </action>
  <verify>
- `test -f man/figures/logo.png && echo "logo.png exists"`
- `test -f man/figures/rbmiUtils.png && echo "original preserved"`
- `grep -q "man/figures/logo.png" README.Rmd && echo "README.Rmd updated"`
- `grep -q "man/figures/logo.png" README.md && echo "README.md updated"`
  </verify>
  <done>
- man/figures/logo.png exists as a copy of the hex logo
- man/figures/rbmiUtils.png is preserved
- Both README.Rmd and README.md reference man/figures/logo.png
  </done>
</task>

<task type="auto">
  <name>Task 2: Generate favicons from logo</name>
  <files>pkgdown/favicon</files>
  <action>
1. Generate favicons using pkgdown. Run in R:
   ```r
   pkgdown::build_favicons(pkg = ".", overwrite = TRUE)
   ```
   This calls the realfavicongenerator.net API and creates the `pkgdown/favicon/` directory with all favicon variants (favicon.ico, apple-touch-icon, android-chrome, etc.).

2. If `pkgdown::build_favicons()` fails due to network issues or API errors, this is acceptable -- favicons are a nice-to-have that GitHub Actions CI will generate on the next pkgdown build. In that case, note the failure but do not block the plan.

3. Verify the `pkgdown/favicon/` directory was created and contains at least `favicon.ico`.

4. Ensure `pkgdown/` is not in `.gitignore` so favicons are committed. Check `.Rbuildignore` includes `^pkgdown$` (standard pattern) so favicons are excluded from the R package build but tracked in git.
  </action>
  <verify>
- `ls pkgdown/favicon/ 2>/dev/null | head -5` shows favicon files
- `grep -q "pkgdown" .Rbuildignore && echo "pkgdown in Rbuildignore"` (should be present or need adding)
  </verify>
  <done>
- pkgdown/favicon/ directory exists with generated favicon files (or documented skip if API unavailable)
- .Rbuildignore includes ^pkgdown$ pattern
  </done>
</task>

</tasks>

<verification>
1. `man/figures/logo.png` exists and is a valid PNG
2. `README.Rmd` and `README.md` both reference `logo.png` not `rbmiUtils.png`
3. `pkgdown/favicon/` directory exists with favicon files
4. Original `man/figures/rbmiUtils.png` is preserved
</verification>

<success_criteria>
- Logo file at standard pkgdown path (man/figures/logo.png)
- README updated to reference the standard logo path
- Favicons generated for browser tab display
- No broken references to the old logo filename
</success_criteria>

<output>
After completion, create `.planning/phases/07-site-polish/07-01-SUMMARY.md`
</output>
