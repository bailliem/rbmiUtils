---
phase: 13-cran-compliance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vignettes/analyse2.Rmd
  - vignettes/data-preparation.Rmd
  - vignettes/diagnostics.Rmd
  - vignettes/efficient-storage.Rmd
  - vignettes/deriving-endpoints.Rmd
autonomous: true

must_haves:
  truths:
    - "All vignettes build without warnings or extraneous console output"
    - "No single test file takes longer than 60 seconds"
    - "Total test suite completes in under 5 minutes"
    - "All examples wrapped in donttest or dontrun complete quickly when run individually"
  artifacts:
    - path: "vignettes/analyse2.Rmd"
      provides: "Warning-suppressed vignette"
      contains: "warning = FALSE"
    - path: "vignettes/data-preparation.Rmd"
      provides: "Warning-suppressed vignette"
      contains: "warning = FALSE"
    - path: "vignettes/diagnostics.Rmd"
      provides: "Warning-suppressed vignette"
      contains: "warning = FALSE"
    - path: "vignettes/efficient-storage.Rmd"
      provides: "Warning-suppressed vignette"
      contains: "warning = FALSE"
    - path: "vignettes/deriving-endpoints.Rmd"
      provides: "Warning-suppressed vignette"
      contains: "warning = FALSE"
  key_links:
    - from: "vignettes/*.Rmd"
      to: "R CMD check"
      via: "Vignette build step"
      pattern: "VignetteBuilder: knitr"
---

<objective>
Ensure all vignettes build cleanly without warnings or extraneous output, and audit test/example timing to confirm CRAN time limits are respected.

Purpose: CRAN checks build all vignettes and run all tests; any warnings, errors, or timeouts cause rejection. This plan ensures a clean check.
Output: Warning-free vignette builds and timing-compliant tests/examples
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/quick/1-fix-vignette-warnings-suppress-stan-comp/1-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Comprehensive vignette warning suppression audit</name>
  <files>
    vignettes/analyse2.Rmd
    vignettes/data-preparation.Rmd
    vignettes/diagnostics.Rmd
    vignettes/efficient-storage.Rmd
    vignettes/deriving-endpoints.Rmd
  </files>
  <action>
Quick Task 1 already fixed Stan warnings in pipeline.Rmd. Now audit ALL six vignettes systematically.

For each vignette, ensure EVERY executable R code chunk (eval != FALSE) has `message = FALSE, warning = FALSE` in its chunk options. This prevents:
- Package loading messages (e.g., "Attaching package: 'dplyr'")
- Stan compilation noise
- Deprecation warnings from dependencies
- Any other console noise that CRAN reviewers flag

**Audit approach:**
1. Open each vignette
2. Find all R code chunks (lines starting with ````{r`)
3. For chunks that are NOT `eval = FALSE` and NOT `include = FALSE`:
   - If they don't have `warning = FALSE`, add it
   - If they don't have `message = FALSE`, add it
4. Exception: chunks with `echo = FALSE, results = "hide"` or similar output-suppressing options are fine as-is

**Already handled:** pipeline.Rmd (Quick Task 1)

**Check each vignette:**
- `analyse2.Rmd` — Has `warning = FALSE` on libraries chunk only. Check if other chunks (draws, impute, analyse, pool, etc.) also need it.
- `data-preparation.Rmd` — Has `warning = FALSE` on libraries chunk. Check remaining chunks.
- `diagnostics.Rmd` — Has `warning = FALSE` on setup chunk. Many chunks are `eval = FALSE`. Check executed chunks.
- `efficient-storage.Rmd` — Has `warning = FALSE` on libraries chunk. Check remaining chunks.
- `deriving-endpoints.Rmd` — Has `warning = FALSE` on several chunks. Check all executed chunks.

For the global knitr setup chunk at the top of each vignette, consider adding `warning = FALSE, message = FALSE` to the global `knitr::opts_chunk$set()` call. This is the cleanest approach as it applies to all chunks by default. If a vignette already uses per-chunk suppression, switching to global is preferred.
  </action>
  <verify>
For each vignette, run: `R -e 'rmarkdown::render("vignettes/FILENAME.Rmd", quiet = TRUE)'` and check for warnings in stderr.
Or: Check that every knitr setup chunk includes `warning = FALSE, message = FALSE` in opts_chunk$set().
  </verify>
  <done>All six vignettes have comprehensive warning and message suppression either globally (via opts_chunk$set) or per-chunk, ensuring zero console noise during R CMD check vignette build.</done>
</task>

<task type="auto">
  <name>Task 2: Test and example timing audit</name>
  <files>
    tests/testthat/test-analyse_mi_data.R
    tests/testthat/test-integration.R
  </files>
  <action>
Audit test execution times to ensure CRAN compliance (no single test file > 60s, total < 10min).

**Step 1: Measure test file execution times**

Run each test file individually and measure wall time:
```r
for (f in list.files("tests/testthat", pattern = "^test-.*\\.R$", full.names = TRUE)) {
  t <- system.time(testthat::test_file(f, reporter = "silent"))
  cat(sprintf("%5.1fs  %s\n", t["elapsed"], basename(f)))
}
```

**Step 2: Identify slow test files**

Flag any file taking > 30 seconds (conservative threshold — CRAN's 60s limit includes overhead).

Likely slow candidates based on file size and complexity:
- `test-data_helpers.R` (821 lines, 158 tests) — data manipulation tests, likely fast
- `test-describe.R` (774 lines, 128 tests) — object introspection, likely fast
- `test-ard_conversion.R` (650 lines, 123 tests) — ARD conversion, likely fast
- `test-analyse_mi_data.R` (662 lines, 77 tests) — may run actual analyses
- `test-integration.R` (419 lines, 38 tests) — integration tests may be slow

**Step 3: If any test file exceeds 30s:**
- Add `skip_on_cran()` to the slowest individual test_that() blocks
- Or split the file into fast/slow portions
- As a last resort, wrap slow tests in `testthat::skip_on_cran()`

**Step 4: Check example timing**

Verify that examples using `\donttest{}` are appropriate:
- `\donttest{}` examples are NOT run during R CMD check (correct for slow examples)
- `\dontrun{}` examples are never run (correct for examples needing MCMC)
- Regular examples must complete in < 5 seconds each

The existing convention (ADMI uses `\donttest{}`, ADEFF uses `\dontrun{}`) was established in v3. Verify it's still appropriate.

**Important:** Only modify test files if timing issues are actually found. If all tests complete within limits, document the timing results but make no changes.
  </action>
  <verify>
Run full test suite with timing: `R -e 'system.time(testthat::test_dir("tests/testthat"))'`
Confirm total elapsed time < 300 seconds (5 minutes).
Confirm no individual file > 60 seconds.
  </verify>
  <done>All test files execute within CRAN time limits (no file > 60s, total suite < 5 min). Example wrapping conventions are verified as appropriate. Any slow tests have skip_on_cran() guards if needed.</done>
</task>

</tasks>

<verification>
1. Build all vignettes: `R -e 'devtools::build_vignettes()'` completes without warnings
2. Run tests: `R -e 'devtools::test()'` passes with no failures and completes in < 5 minutes
3. No vignette output contains Stan compilation messages or deprecation warnings
</verification>

<success_criteria>
- Zero warnings or extraneous messages during vignette builds
- All test files complete within CRAN's 60-second per-file limit
- Total test suite under 5 minutes
- Example timing conventions verified
</success_criteria>

<output>
After completion, create `.planning/phases/13-cran-compliance/13-02-SUMMARY.md`
</output>
