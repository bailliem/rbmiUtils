---
phase: 13-cran-compliance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DESCRIPTION
  - NEWS.md
autonomous: true

must_haves:
  truths:
    - "DESCRIPTION Title field uses proper Title Case and no trailing period"
    - "DESCRIPTION Description field is a proper paragraph (no single quotes around package names except CRAN-mandated ones)"
    - "No explicit Maintainer field (derived from Authors@R cre role)"
    - "NEWS.md has consistent heading format for all version entries"
    - "NEWS.md version numbers reflect actual release history"
  artifacts:
    - path: "DESCRIPTION"
      provides: "CRAN-compliant package metadata"
      contains: "Authors@R"
    - path: "NEWS.md"
      provides: "Consistently formatted changelog"
      contains: "# rbmiUtils"
  key_links:
    - from: "DESCRIPTION"
      to: "NEWS.md"
      via: "Version number consistency"
      pattern: "Version:"
---

<objective>
Audit and fix DESCRIPTION metadata for CRAN policy compliance, and clean up NEWS.md formatting for consistency across all version entries.

Purpose: CRAN's automated checks and human reviewers flag non-compliant DESCRIPTION fields and poorly formatted NEWS files. Both must be clean before submission.
Output: CRAN-compliant DESCRIPTION and consistently formatted NEWS.md
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESCRIPTION
@NEWS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DESCRIPTION CRAN policy audit and fixes</name>
  <files>DESCRIPTION</files>
  <action>
Audit and fix the DESCRIPTION file for CRAN policy compliance:

1. **Remove explicit Maintainer field** — CRAN derives Maintainer from Authors@R (cre role). Having both can trigger a NOTE. Delete the `Maintainer: Mark Baillie <bailliem@gmail.com>` line.

2. **Validate Title field** — Current: "Utility Functions to Support and Extend the 'rbmi' Package". Check Title Case is correct (every word capitalized except articles/prepositions/conjunctions). The current title looks correct. Ensure no trailing period.

3. **Validate Description field** — Must be a proper paragraph. Package names in single quotes ('rbmi' is already correct). Check it doesn't start with "This package" or the package name. Current description looks compliant but verify it reads well.

4. **Check Authors@R** — Verify all roles are valid CRAN roles (aut, cre, cph, ctb are all fine). Ensure exactly one cre.

5. **Check License** — "GPL (>= 3)" is a valid CRAN license string.

6. **Check URL and BugReports** — Verify URLs are properly formatted and point to valid GitHub locations.

7. **Bump Version to 0.4.0** — The v4 milestone is CRAN release readiness. Update Version field from 0.2.2 to 0.4.0 to align with the milestone versioning documented in the roadmap (NEWS already has content mapped to 0.2.0 = v2, 0.2.1 = v2, 0.2.2 = v3 — renumber to 0.4.0 for the CRAN submission version).

Actually — DO NOT bump version. The version bump should happen in Phase 14 as the final step before submission. Leave Version as 0.2.2 for now and only clean up metadata.

8. **Verify no deprecated packages in Imports** — `assertthat` is soft-deprecated but still on CRAN and functional. Leave it for now (removing would be a feature change, not allowed in v4). If it triggers a NOTE in Phase 14, address then.
  </action>
  <verify>
Run: `R -e 'desc::desc_get("Title")'; R -e 'desc::desc_get("Description")'` to check fields.
Verify no Maintainer line in DESCRIPTION: `grep -c "^Maintainer:" DESCRIPTION` should return 0.
  </verify>
  <done>DESCRIPTION has no explicit Maintainer field, Title/Description/Authors@R/License/URL/BugReports all follow CRAN policy formatting rules, no NOTE-triggering metadata issues remain.</done>
</task>

<task type="auto">
  <name>Task 2: NEWS.md formatting cleanup for CRAN standards</name>
  <files>NEWS.md</files>
  <action>
Clean up NEWS.md so all version entries use consistent CRAN-standard formatting.

CRAN NEWS.md conventions (from R-exts manual and tidyverse practice):
- Top-level headings: `# pkgname version` (e.g., `# rbmiUtils 0.2.2`)
- Sub-sections with `##` headers: `## New Features`, `## Bug Fixes`, `## Improvements`, `## Breaking Changes`, `## Dependencies`
- Bullet items with `*` (already used)
- Each version entry should have at least one `##` section header
- Entries ordered newest to oldest (already correct)

Current issues to fix:
1. **0.1.9** — Has bare bullets with no section header. Wrap them in `## New Features` and `## Improvements` as appropriate (the storage functions and validation are features; formatting utils and datasets are features; pkgdown is improvements).
2. **0.1.8** — Only says "CRAN release: 2026-01-24". Add a brief note or keep as `## CRAN Release` with the date as a note.
3. **0.1.7** — Has bare bullets. Wrap in `## Improvements`.
4. **0.1.6** — Bare text. Wrap in `## Improvements`.
5. **0.1.4** — Only "CRAN release: 2025-05-23". Keep minimal but add `## CRAN Release` header.
6. **0.1.0** — Has bare bullets. Wrap in appropriate `##` sections.

Ensure all `*` bullets are indented consistently and have blank lines between sections. The top three entries (0.2.2, 0.2.1, 0.2.0) already have proper formatting — use them as the template.
  </action>
  <verify>
Visually inspect NEWS.md to confirm every version block has `# rbmiUtils X.Y.Z` followed by `##` section headers.
Run: `grep "^# " NEWS.md` should show only version headers.
Run: `grep "^## " NEWS.md` should show section headers under each version.
  </verify>
  <done>All NEWS.md version entries (0.1.0 through 0.2.2) have consistent formatting with `#` version headers and `##` section headers, matching CRAN conventions.</done>
</task>

</tasks>

<verification>
1. `grep -c "^Maintainer:" DESCRIPTION` returns 0
2. All NEWS.md version entries have `##` section headers
3. DESCRIPTION fields are syntactically valid: `R -e 'read.dcf("DESCRIPTION")'` succeeds
</verification>

<success_criteria>
- DESCRIPTION passes CRAN metadata policy checks with no NOTEs from metadata fields
- NEWS.md is consistently formatted across all version entries
- No functional changes to package behavior
</success_criteria>

<output>
After completion, create `.planning/phases/13-cran-compliance/13-01-SUMMARY.md`
</output>
