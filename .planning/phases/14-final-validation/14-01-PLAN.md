---
phase: 14-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .Rbuildignore
  - .gitignore
  - DESCRIPTION
autonomous: true

must_haves:
  truths:
    - "R CMD check --as-cran returns 0 errors, 0 warnings, 0 notes"
    - "Package tarball builds cleanly without unexpected files"
    - "All vignettes, tests, and examples execute successfully during check"
  artifacts:
    - path: ".Rbuildignore"
      provides: "Excludes .planning, Rplots.pdf, and any other non-standard files from tarball"
      contains: "\\.planning"
    - path: "DESCRIPTION"
      provides: "Version bump to 0.3.0 for CRAN submission"
      contains: "Version: 0.3.0"
  key_links:
    - from: ".Rbuildignore"
      to: "R CMD build"
      via: "File exclusion patterns"
      pattern: "\\.planning"
---

<objective>
Run R CMD check --as-cran to zero errors, zero warnings, zero notes. Fix any issues found during the check. Bump version to 0.3.0 as the final pre-submission step.

Purpose: This is the gate between development and CRAN submission. The package must pass the most stringent check with a completely clean bill of health.
Output: A package that passes `R CMD check --as-cran` with status 0/0/0 and version 0.3.0 ready for submission.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-cran-compliance/13-01-SUMMARY.md
@.planning/phases/13-cran-compliance/13-02-SUMMARY.md
@DESCRIPTION
@.Rbuildignore
@.gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-check cleanup -- Rbuildignore, gitignore, and version bump</name>
  <files>.Rbuildignore, .gitignore, DESCRIPTION</files>
  <action>
Three items to fix before running R CMD check:

1. **Add `.planning` to .Rbuildignore.** The `.planning` directory is NOT currently excluded from the package tarball. Add the line `^\.planning$` to `.Rbuildignore`. This is critical -- without it, R CMD check will emit a NOTE about non-standard top-level files.

2. **Handle tests/testthat/Rplots.pdf.** This file is an artifact from test runs (ggplot2 creates it). Add `Rplots\.pdf$` to `.Rbuildignore` so it is excluded from the tarball if present during build. Also add `Rplots.pdf` to `.gitignore` so it stops showing as untracked.

3. **Bump version from 0.2.2 to 0.3.0 in DESCRIPTION.** Per decision from Phase 13: "Left version at 0.2.2 for now; bump deferred to Phase 14 final step." Update the Version field to 0.3.0. Also add a `Date` field with today's date (YYYY-MM-DD format) -- CRAN prefers a Date field.

Do NOT change any other DESCRIPTION fields -- Phase 13 already validated CRAN compliance of Title, Description, Authors@R, License, URL, BugReports.
  </action>
  <verify>
Run these checks:
- `grep '\.planning' .Rbuildignore` confirms the pattern is present
- `grep 'Rplots' .Rbuildignore` confirms the pattern is present
- `grep 'Rplots' .gitignore` confirms the pattern is present
- `Rscript -e 'read.dcf("DESCRIPTION", fields=c("Version","Date"))'` shows Version 0.3.0 and Date field
  </verify>
  <done>
.Rbuildignore excludes .planning and Rplots.pdf; .gitignore excludes Rplots.pdf; DESCRIPTION shows Version 0.3.0 with Date field.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run R CMD check --as-cran and fix any issues</name>
  <files>Any files that need fixing based on check output</files>
  <action>
Build the package tarball and run the full CRAN check:

```bash
cd /Users/bailliem/R-projects/rbmiUtils-gsd
R CMD build .
R CMD check --as-cran rbmiUtils_0.3.0.tar.gz
```

**Expected outcome:** 0 errors, 0 warnings, 0 notes.

**If issues are found, fix them.** Common CRAN check issues and fixes:

- **NOTE about non-standard files:** Something missing from .Rbuildignore -- add it.
- **NOTE about package size:** Check if data/ or inst/ is oversized (data is 64KB total, should be fine).
- **WARNING about undocumented objects:** Run `devtools::document()` then rebuild.
- **NOTE about "no visible binding":** Add `utils::globalVariables()` for NSE variables in dplyr pipelines (check R/globals.R or similar).
- **NOTE about deprecated packages in Imports:** assertthat is kept per decision (out of scope to remove).
- **NOTE about LazyData without data:** We have data/ so this is fine.

After fixing any issues, rebuild and recheck until clean. The check may take several minutes due to vignette building (Stan MCMC).

**Important:** If any NOTE appears about assertthat being deprecated/archived, document it but do NOT remove assertthat -- per locked decision, that is out of v4 scope. If this is the only NOTE, document it as an accepted known issue.

Clean up after: remove the tarball and the .Rcheck directory.
  </action>
  <verify>
The final `R CMD check --as-cran` output ends with:
```
Status: OK
```
or at minimum shows 0 errors and 0 warnings. Capture the full check output for the summary.
  </verify>
  <done>
R CMD check --as-cran passes with 0 errors, 0 warnings, and 0 notes (or only an accepted assertthat NOTE if unavoidable). Check directory and tarball cleaned up.
  </done>
</task>

</tasks>

<verification>
1. `R CMD check --as-cran` final status shows 0 errors, 0 warnings, 0 notes
2. Package tarball builds without including .planning directory or Rplots.pdf
3. DESCRIPTION shows Version: 0.3.0
4. All vignettes built successfully during check
5. All tests passed during check
</verification>

<success_criteria>
The package passes `R CMD check --as-cran` with a completely clean result (Status: OK). The package is at version 0.3.0 and ready for CRAN submission.
</success_criteria>

<output>
After completion, create `.planning/phases/14-final-validation/14-01-SUMMARY.md`
</output>
