---
phase: 12-content-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vignettes/deriving-endpoints.Rmd
  - _pkgdown.yml
autonomous: true

must_haves:
  truths:
    - "A standalone vignette for binary responder analysis exists at vignettes/deriving-endpoints.Rmd"
    - "The vignette demonstrates threshold-based responder (>=50% improvement) AND clinical cutoff responder (e.g., HAMD <=10 equivalent)"
    - "The vignette shows ARD flow using pool_to_ard()"
    - "The vignette builds without errors"
    - "The vignette appears in the pkgdown articles listing"
  artifacts:
    - path: "vignettes/deriving-endpoints.Rmd"
      provides: "Standalone binary responder vignette"
      min_lines: 150
      contains: "VignetteIndexEntry"
    - path: "_pkgdown.yml"
      provides: "Updated pkgdown nav with new article"
      contains: "deriving-endpoints"
  key_links:
    - from: "vignettes/deriving-endpoints.Rmd"
      to: "ADMI dataset"
      via: "data(ADMI, package = rbmiUtils)"
      pattern: "data.*ADMI.*rbmiUtils"
    - from: "vignettes/deriving-endpoints.Rmd"
      to: "pool_to_ard"
      via: "ARD conversion"
      pattern: "pool_to_ard"
    - from: "_pkgdown.yml"
      to: "vignettes/deriving-endpoints.Rmd"
      via: "articles menu entry"
      pattern: "deriving-endpoints"
---

<objective>
Create a standalone vignette titled "Deriving Endpoints from Imputed Data" that demonstrates how to extract imputed datasets, define binary responder endpoints, and reanalyse them using rbmi's analyse/pool machinery.

Purpose: Fulfils DOC-01 -- users need a dedicated guide for binary responder workflows separate from the pipeline vignette.
Output: `vignettes/deriving-endpoints.Rmd` and updated `_pkgdown.yml`.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@vignettes/pipeline.Rmd
@R/analysis_utils.R
@R/ard_conversion.R
@R/analyse_mi_data.R
@_pkgdown.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create deriving-endpoints vignette</name>
  <files>vignettes/deriving-endpoints.Rmd</files>
  <action>
Create `vignettes/deriving-endpoints.Rmd` with title "Deriving Endpoints from Imputed Data" and VignetteIndexEntry matching.

Structure (per user decisions):

1. **YAML front matter** -- html_vignette output, toc: true, toc_depth: 3, number_sections: true. Same knitr opts_chunk as pipeline.Rmd (collapse=TRUE, comment="#>").

2. **Introduction** (brief) -- Explain that imputed data from rbmi can be reused for derived endpoints. Link to the pipeline vignette for the full rbmi workflow. State this vignette assumes familiarity with rbmi core concepts (draws, impute, analyse, pool). Practical tutorial tone: "Next, we extract the imputed datasets..."

3. **Prerequisites / Setup** section -- `library(rbmi)`, `library(rbmiUtils)`, `library(dplyr)`. Load the pre-built ADMI dataset: `data("ADMI", package = "rbmiUtils")`. Factor conversion for TRT, USUBJID, AVISIT, STRATA, REGION (copy pattern from pipeline.Rmd binary appendix). Define vars_binary using set_vars with outcome = "CRIT1FLN". Inline comments explaining key steps.

4. **Threshold-Based Responder (CHG > 3)** section -- The ADMI dataset already has CRIT1FLN column (CHG > 3). Show that this is already present. Demonstrate analysing it with analyse_mi_data() + gcomp_responder_multi(). Pool with pool(). Show tidy_pool_obj() output. Show ARD flow: pool_to_ard(). End with a formatted efficacy_table() showing responder rates.

5. **Clinical Cutoff Responder** section -- Demonstrate deriving a NEW binary endpoint from the continuous CHG column in the imputed data. For example, define a "clinically meaningful improvement" as CHG > 5 (a higher threshold). Mutate ADMI to create a new binary column. Re-run analyse_mi_data() + gcomp_responder_multi() + pool(). Show the results table. This demonstrates the FLEXIBILITY of imputed data reuse -- the key teaching point.

6. **Storing Results as ARD** section -- Show pool_to_ard() on one of the pool objects. Brief explanation of ARD format for downstream use.

7. **Caveats** section (brief) -- Per user decision: include brief caveats on assumptions/limitations of binary responder approach from imputed data. Key points: (a) binary endpoints derived post-hoc from continuous imputation carry the assumptions of the continuous model, (b) responder thresholds should be pre-specified in the statistical analysis plan, (c) results are conditional on the imputation model and reference-based assumptions.

Key constraints per user decisions:
- Post-imputation focus: assume rbmi already run, use pre-built ADMI
- Endpoint-only analysis (final visit filtering NOT needed -- gcomp_responder_multi handles all visits)
- No forest plot or continuous comparison
- No citations or academic references
- No comparison with continuous primary analysis
- Use rbmi's built-in analyse/pool -- show WHAT to pass, not HOW pooling works
- Output table is self-explanatory -- no interpretation walkthrough
- Serves both trial statisticians and R programmers

For CRAN timing: use eval=TRUE for all chunks since ADMI is pre-computed (no draws/impute needed). The gcomp_responder_multi + pool steps are fast (~seconds). Use `message=FALSE, warning=FALSE` on chunks that may produce beeca/Stan messages. If any chunk is slow, wrap with eval=FALSE and show pre-computed output inline.

Use `eval = requireNamespace("gt", quietly = TRUE)` guard on efficacy_table() chunks (same pattern as pipeline.Rmd).
  </action>
  <verify>
Run `Rscript -e "rmarkdown::render('vignettes/deriving-endpoints.Rmd', output_dir=tempdir())"` and confirm exit code 0 with no errors. Verify the output HTML contains both "Threshold" and "Clinical Cutoff" sections.
  </verify>
  <done>
Vignette builds without errors. Contains threshold-based responder section, clinical cutoff responder section, ARD flow, and caveats. Uses ADMI pre-built data throughout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add vignette to pkgdown navigation</name>
  <files>_pkgdown.yml</files>
  <action>
Read `_pkgdown.yml`. In the navbar > components > articles > menu section, add the new vignette entry. Place it under the "Workflow Guides" group (after "Efficient Storage of Imputed Data"):

```yaml
        - text: "Deriving Endpoints from Imputed Data"
          href: articles/deriving-endpoints.html
```

This ensures the vignette appears in the pkgdown articles dropdown.
  </action>
  <verify>
Run `Rscript -e "pkgdown::check_pkgdown('.')"` or visually inspect `_pkgdown.yml` YAML is valid with `Rscript -e "yaml::read_yaml('_pkgdown.yml')"` -- should parse without error.
  </verify>
  <done>
`_pkgdown.yml` contains "deriving-endpoints" article entry under Workflow Guides. YAML parses without error.
  </done>
</task>

</tasks>

<verification>
1. `Rscript -e "rmarkdown::render('vignettes/deriving-endpoints.Rmd', output_dir=tempdir())"` exits 0
2. `Rscript -e "yaml::read_yaml('_pkgdown.yml')"` parses without error
3. Built HTML contains both responder endpoint types
4. Vignette contains pool_to_ard() call
5. Existing tests still pass: `Rscript -e "testthat::test_dir('tests/testthat')"`
</verification>

<success_criteria>
- Standalone vignette exists at vignettes/deriving-endpoints.Rmd
- Vignette builds cleanly without errors or warnings
- Demonstrates both threshold-based AND clinical cutoff responder definitions
- Shows ARD conversion workflow
- Includes brief caveats section
- Listed in pkgdown articles navigation
</success_criteria>

<output>
After completion, create `.planning/phases/12-content-visual-polish/12-01-SUMMARY.md`
</output>
