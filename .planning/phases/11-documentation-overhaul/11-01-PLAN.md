---
phase: 11-documentation-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.Rmd
  - README.md
  - _pkgdown.yml
  - NEWS.md
  - DESCRIPTION
autonomous: true

must_haves:
  truths:
    - "README Quick Start shows complete ADEFF-through-pipeline workflow with data prep, draws, impute, get_imputed_data, analyse_mi_data, pool, efficacy_table, plot_forest steps"
    - "README Key Features lists describe_draws(), describe_imputation(), and enriched pool_to_ard()"
    - "README Learn More section links to new diagnostics vignette"
    - "NEWS.md has v0.3.0 section with all phase 8/9/10 features documented"
    - "DESCRIPTION version is 0.3.0"
    - "_pkgdown.yml has describe_draws and describe_imputation in reference sections"
  artifacts:
    - path: "README.Rmd"
      provides: "Source for README with ADEFF pipeline workflow and v3 features"
      contains: "describe_draws"
    - path: "NEWS.md"
      provides: "Changelog with v0.3.0 entries"
      contains: "rbmiUtils 0.3.0"
    - path: "_pkgdown.yml"
      provides: "Updated reference sections and navbar with diagnostics vignette"
      contains: "describe_draws"
    - path: "DESCRIPTION"
      provides: "Version bumped to 0.3.0"
      contains: "Version: 0.3.0"
  key_links:
    - from: "README.Rmd"
      to: "man/figures/"
      via: "static image references"
      pattern: "man/figures/README-"
    - from: "_pkgdown.yml"
      to: "vignettes/diagnostics.Rmd"
      via: "navbar articles menu"
      pattern: "diagnostics"
    - from: "NEWS.md"
      to: "DESCRIPTION"
      via: "version consistency"
      pattern: "0\\.3\\.0"
---

<objective>
Overhaul README.Rmd with a complete ADEFF-through-pipeline workflow replacing the current minimal Quick Start, add v3 features to Key Features, update _pkgdown.yml with new function categories and navbar links, write NEWS.md v0.3.0 entries, and bump DESCRIPTION version to 0.3.0.

Purpose: README is the package's front door and must showcase the full v3 feature set with a realistic clinical trial workflow. NEWS.md and version bump complete the release documentation.
Output: Updated README.Rmd/README.md, _pkgdown.yml, NEWS.md, DESCRIPTION
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation-overhaul/11-RESEARCH.md

@README.Rmd
@_pkgdown.yml
@NEWS.md
@DESCRIPTION
@vignettes/pipeline.Rmd
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.Rmd with complete ADEFF pipeline workflow and v3 features</name>
  <files>README.Rmd, README.md</files>
  <action>
Edit README.Rmd with the following changes:

1. **Quick Start section**: Replace the current minimal 4-line example with a COMPLETE ADEFF-through-pipeline workflow. This must be a realistic workflow, NOT a teaser. Use `eval = FALSE` code blocks (README images are pre-rendered). The code should be approximately 40-60 lines showing ALL pipeline steps:

```r
library(rbmiUtils)
library(rbmi)
library(dplyr)

# Load example efficacy dataset and prepare factors
data("ADEFF", package = "rbmiUtils")
ADEFF <- ADEFF |>
  mutate(
    TRT = factor(TRT01P, levels = c("Placebo", "Drug A")),
    USUBJID = factor(USUBJID),
    AVISIT = factor(AVISIT, levels = c("Week 24", "Week 48"))
  )

# Define analysis variables
vars <- set_vars(
  subjid = "USUBJID",
  visit = "AVISIT",
  group = "TRT",
  outcome = "CHG",
  covariates = c("BASE", "STRATA", "REGION")
)

# Configure Bayesian imputation method
method <- method_bayes(
  n_samples = 100,
  control = control_bayes(warmup = 200, thin = 2)
)

# Step 1: Fit imputation model (draws)
dat <- ADEFF |> select(USUBJID, STRATA, REGION, TRT, BASE, CHG, AVISIT)
draws_obj <- draws(data = dat, vars = vars, method = method)

# Step 2: Generate imputed datasets
impute_obj <- impute(
  draws_obj,
  references = c("Placebo" = "Placebo", "Drug A" = "Placebo")
)

# Step 3: Extract stacked imputed data
ADMI <- get_imputed_data(impute_obj)

# Step 4: Analyse each imputed dataset
ana_obj <- analyse_mi_data(data = ADMI, vars = vars, method = method, fun = ancova)

# Step 5: Pool results using Rubin's rules
pool_obj <- pool(ana_obj)

# Publication-ready outputs
efficacy_table(pool_obj, arm_labels = c(ref = "Placebo", alt = "Drug A"))
plot_forest(pool_obj, arm_labels = c(ref = "Placebo", alt = "Drug A"))
```

Update the introductory text above the code block from "Starting from an rbmi pool object" to something like: "rbmiUtils extends the rbmi pipeline from raw data to publication-ready outputs. Here is the complete workflow using the bundled ADEFF dataset:". Remove the old "Starting from an [rbmi]... pool object" framing since we now show the full pipeline, not just post-pool steps.

2. **Key Features section**: Add these bullet points after the existing `pool_to_ard()` entry:
   - `describe_draws()` -- inspect draws objects (method, samples, convergence diagnostics)
   - `describe_imputation()` -- inspect imputation objects (method, M, missingness breakdown)
   - Update the existing `pool_to_ard()` entry to mention "with optional MI diagnostic enrichment (FMI, lambda, RIV)"

3. **Learn More section**: Add a third bullet linking to the diagnostics vignette:
   - `[MI Diagnostics and Describe Helpers](https://openpharma.github.io/rbmiUtils/articles/diagnostics.html)` -- inspecting draws, imputations, and MI diagnostic statistics

4. After editing README.Rmd, render it to regenerate README.md. Run: `Rscript -e 'rmarkdown::render("README.Rmd", output_format = "github_document")'` from the project root. Since ALL code chunks use `eval=FALSE`, no R package execution occurs during rendering -- it simply converts the Rmd markup and code fences to markdown. This render MUST succeed. If it fails unexpectedly, diagnose and fix the Rmd syntax error causing the failure, then re-render until README.md is produced.

Important: Do NOT change the image references (man/figures/README-forest-plot-1.png etc.) -- those stay as-is and will be regenerated in plan 11-03.
  </action>
  <verify>
- `grep -c "describe_draws" README.Rmd` returns >= 1
- `grep -c "describe_imputation" README.Rmd` returns >= 1
- `grep -c "ADEFF" README.Rmd` returns >= 1
- `grep -c "draws_obj" README.Rmd` returns >= 1 (proves full pipeline, not just post-pool)
- `grep -c "impute_obj" README.Rmd` returns >= 1 (proves impute step shown)
- `grep -c "get_imputed_data" README.Rmd` returns >= 1 (proves extraction step shown)
- `grep -c "analyse_mi_data" README.Rmd` returns >= 1 (proves analysis step shown)
- `grep -c "diagnostics.html" README.Rmd` returns >= 1
- README.md exists and has been freshly rendered from README.Rmd
  </verify>
  <done>README.Rmd shows complete ADEFF-through-pipeline workflow in Quick Start with ALL steps (data prep, draws, impute, get_imputed_data, analyse_mi_data, pool, efficacy_table, plot_forest), lists all v3 features including describe helpers and enriched pool_to_ard, and links to diagnostics vignette in Learn More. README.md has been rendered from the updated Rmd.</done>
</task>

<task type="auto">
  <name>Task 2: Update _pkgdown.yml, NEWS.md, and DESCRIPTION version</name>
  <files>_pkgdown.yml, NEWS.md, DESCRIPTION</files>
  <action>
**_pkgdown.yml changes:**

1. Add a new reference section "Introspection" AFTER the "Reporting" section (line 63, after the `plot_forest` entry) and BEFORE the "Formatting" section (currently starting at line 64). Insert:
   ```yaml
   - title: "Introspection"
     desc: "Inspect draws, imputations, and MI diagnostics"
     contents:
       - describe_draws
       - describe_imputation
   ```

2. Add the diagnostics vignette to the navbar articles menu. In the `navbar.components.articles.menu` list, AFTER the last entry (line 37, the "Efficient Storage of Imputed Data" entry), add:
   ```yaml
   - text: "-------"
   - text: "Diagnostics"
   - text: "MI Diagnostics and Describe Helpers"
     href: articles/diagnostics.html
   ```

3. The `starts_with("print.")` in "Print & Summary Methods" will automatically pick up `print.describe_draws` and `print.describe_imputation` -- no changes needed there.

**NEWS.md changes:**

Add a new `# rbmiUtils 0.3.0` section at the TOP of NEWS.md (before the existing 0.2.0 section). Content:

```markdown
# rbmiUtils 0.3.0

## New Features

* `describe_draws()` extracts structured metadata from rbmi draws objects,
  including method type, formula, sample count, and (for Bayesian methods)
  MCMC convergence diagnostics (ESS, Rhat).
* `describe_imputation()` extracts imputation metadata including method,
  number of imputations (M), reference arm mappings, and a missingness
  breakdown by visit and treatment arm.
* `pool_to_ard()` gains an `analysis_obj` parameter that enriches the ARD
  with MI diagnostic statistics (FMI, lambda, RIV, Barnard-Rubin adjusted
  df, relative efficiency) when the pooling method is Rubin's rules.

## Improvements

* `efficacy_table()` gains `font_family`, `font_size`, and `row_padding`
  parameters for publication-ready table styling.
* `plot_forest()` gains `font_family` and `panel_widths` parameters for
  customizable typography and panel layout.
* `plot_forest()` left panel now uses left-aligned text (hjust=0) for
  consistent positioning regardless of label length.
* Non-Rubin pooling methods now emit an informative `cli::cli_inform()`
  message when MI diagnostics are not applicable, rather than returning
  NA rows.
```

**DESCRIPTION changes:**

Change `Version: 0.1.9` to `Version: 0.3.0`.
  </action>
  <verify>
- `grep "Version: 0.3.0" DESCRIPTION` returns a match
- `grep "rbmiUtils 0.3.0" NEWS.md` returns a match
- `grep "describe_draws" _pkgdown.yml` returns a match
- `grep "Introspection" _pkgdown.yml` returns a match
- `grep "diagnostics.html" _pkgdown.yml` returns a match
  </verify>
  <done>_pkgdown.yml has Introspection reference section with describe_* functions and navbar link to diagnostics vignette; NEWS.md has complete v0.3.0 section; DESCRIPTION version is 0.3.0</done>
</task>

</tasks>

<verification>
- README.Rmd contains complete ADEFF-through-pipeline workflow with all steps, v3 feature bullets, diagnostics vignette link
- _pkgdown.yml has Introspection section and diagnostics navbar entry
- NEWS.md v0.3.0 section documents all phase 8/9/10 additions
- DESCRIPTION Version field is 0.3.0
- No broken cross-references between files
</verification>

<success_criteria>
- README demonstrates complete clinical trial workflow from ADEFF through full pipeline (draws, impute, get_imputed_data, analyse_mi_data, pool, efficacy_table, plot_forest)
- All v3 exported functions mentioned in README Key Features
- NEWS.md and DESCRIPTION versions are consistent at 0.3.0
- _pkgdown.yml properly categorizes all exported functions including new describe_* helpers
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-overhaul/11-01-SUMMARY.md`
</output>
