---
phase: 11-documentation-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - R/ard_conversion.R
  - R/efficacy_table.R
  - R/plot_forest.R
  - R/describe.R
autonomous: true

must_haves:
  truths:
    - "pool_to_ard() example uses executable ADMI pipeline code inside \\donttest{}, not commented-out pseudocode"
    - "efficacy_table() example uses executable ADMI pipeline code inside \\donttest{}, not commented-out pseudocode"
    - "plot_forest() example uses executable ADMI pipeline code inside \\donttest{}, not commented-out pseudocode"
    - "describe_draws() example uses realistic ADEFF code inside \\dontrun{} showing full pipeline from data to describe_draws()"
    - "describe_imputation() example uses realistic ADEFF code inside \\dontrun{} showing full pipeline from data to describe_imputation()"
  artifacts:
    - path: "R/ard_conversion.R"
      provides: "Executable pool_to_ard() example with ADMI data"
      contains: "analyse_mi_data"
    - path: "R/efficacy_table.R"
      provides: "Executable efficacy_table() example with ADMI data"
      contains: "analyse_mi_data"
    - path: "R/plot_forest.R"
      provides: "Executable plot_forest() example with ADMI data"
      contains: "analyse_mi_data"
    - path: "R/describe.R"
      provides: "Realistic describe_draws/describe_imputation examples with ADEFF"
      contains: "ADEFF"
  key_links:
    - from: "R/ard_conversion.R"
      to: "ADMI dataset"
      via: "@examples \\donttest{} block"
      pattern: "data.*ADMI"
    - from: "R/describe.R"
      to: "ADEFF dataset"
      via: "@examples \\dontrun{} block"
      pattern: "data.*ADEFF"
---

<objective>
Upgrade function @examples in pool_to_ard(), efficacy_table(), plot_forest(), describe_draws(), and describe_imputation() from commented-out pseudocode to executable (or realistic \dontrun) examples using ADMI/ADEFF package datasets.

Purpose: Function help pages are the primary reference for users. Commented-out pseudocode provides no value -- executable examples demonstrate real usage patterns and can be tested.
Output: Updated @examples blocks in 4 R source files
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-documentation-overhaul/11-RESEARCH.md

@R/ard_conversion.R
@R/efficacy_table.R
@R/plot_forest.R
@R/describe.R
@R/analyse_mi_data.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade pool_to_ard, efficacy_table, and plot_forest examples to executable code</name>
  <files>R/ard_conversion.R, R/efficacy_table.R, R/plot_forest.R</files>
  <action>
Replace the commented-out pseudocode in @examples blocks with executable \donttest{} code using the ADMI dataset. All three functions share the same pipeline setup, so the pattern is consistent.

IMPORTANT: These examples use `analyse_mi_data(ADMI, vars, method, fun = ancova)` which works DIRECTLY with pre-imputed ADMI data. This is CORRECT -- analyse_mi_data() does NOT need draws() or impute() or data_ice. It takes a data frame with an IMPID column (like ADMI) and applies the analysis function to each imputation. Read R/analyse_mi_data.R to confirm: signature is `analyse_mi_data(data, vars, method, fun, delta, ...)`.

**R/ard_conversion.R -- pool_to_ard() example:**
Replace the current @examples block (lines ~55-65) with:
```r
#' @examples
#' \donttest{
#' if (requireNamespace("cards", quietly = TRUE)) {
#'   library(rbmi)
#'   data("ADMI", package = "rbmiUtils")
#'   ADMI$TRT <- factor(ADMI$TRT, levels = c("Placebo", "Drug A"))
#'   ADMI$USUBJID <- factor(ADMI$USUBJID)
#'   ADMI$AVISIT <- factor(ADMI$AVISIT)
#'
#'   vars <- set_vars(
#'     subjid = "USUBJID", visit = "AVISIT", group = "TRT",
#'     outcome = "CHG", covariates = c("BASE", "STRATA", "REGION")
#'   )
#'   method <- method_bayes(
#'     n_samples = 20,
#'     control = control_bayes(warmup = 20, thin = 1)
#'   )
#'
#'   ana_obj <- analyse_mi_data(ADMI, vars, method, fun = ancova)
#'   pool_obj <- pool(ana_obj)
#'
#'   # Base ARD
#'   ard <- pool_to_ard(pool_obj)
#'
#'   # Enriched ARD with MI diagnostics (FMI, lambda, RIV, df)
#'   ard_diag <- pool_to_ard(pool_obj, analysis_obj = ana_obj)
#' }
#' }
```

**R/efficacy_table.R -- efficacy_table() example:**
Replace the current @examples block (lines ~58-73) with:
```r
#' @examples
#' \donttest{
#' if (requireNamespace("gt", quietly = TRUE)) {
#'   library(rbmi)
#'   data("ADMI", package = "rbmiUtils")
#'   ADMI$TRT <- factor(ADMI$TRT, levels = c("Placebo", "Drug A"))
#'   ADMI$USUBJID <- factor(ADMI$USUBJID)
#'   ADMI$AVISIT <- factor(ADMI$AVISIT)
#'
#'   vars <- set_vars(
#'     subjid = "USUBJID", visit = "AVISIT", group = "TRT",
#'     outcome = "CHG", covariates = c("BASE", "STRATA", "REGION")
#'   )
#'   method <- method_bayes(
#'     n_samples = 20,
#'     control = control_bayes(warmup = 20, thin = 1)
#'   )
#'
#'   ana_obj <- analyse_mi_data(ADMI, vars, method, fun = ancova)
#'   pool_obj <- pool(ana_obj)
#'
#'   # Basic table
#'   tbl <- efficacy_table(pool_obj)
#'
#'   # Publication-styled table
#'   efficacy_table(
#'     pool_obj,
#'     title = "Table 14.2.1: ANCOVA of Change from Baseline",
#'     subtitle = "Mixed Model for Repeated Measures",
#'     arm_labels = c(ref = "Placebo", alt = "Drug A"),
#'     font_size = 12,
#'     row_padding = 4
#'   )
#' }
#' }
```

**R/plot_forest.R -- plot_forest() example:**
Replace the current @examples block (lines ~78-94) with:
```r
#' @examples
#' \donttest{
#' if (requireNamespace("ggplot2", quietly = TRUE) &&
#'     requireNamespace("patchwork", quietly = TRUE)) {
#'   library(rbmi)
#'   data("ADMI", package = "rbmiUtils")
#'   ADMI$TRT <- factor(ADMI$TRT, levels = c("Placebo", "Drug A"))
#'   ADMI$USUBJID <- factor(ADMI$USUBJID)
#'   ADMI$AVISIT <- factor(ADMI$AVISIT)
#'
#'   vars <- set_vars(
#'     subjid = "USUBJID", visit = "AVISIT", group = "TRT",
#'     outcome = "CHG", covariates = c("BASE", "STRATA", "REGION")
#'   )
#'   method <- method_bayes(
#'     n_samples = 20,
#'     control = control_bayes(warmup = 20, thin = 1)
#'   )
#'
#'   ana_obj <- analyse_mi_data(ADMI, vars, method, fun = ancova)
#'   pool_obj <- pool(ana_obj)
#'
#'   # Treatment difference forest plot
#'   plot_forest(pool_obj, arm_labels = c(ref = "Placebo", alt = "Drug A"))
#'
#'   # LSM display with custom panel widths
#'   plot_forest(
#'     pool_obj,
#'     display = "lsm",
#'     arm_labels = c(ref = "Placebo", alt = "Drug A"),
#'     title = "LS Mean Estimates by Visit",
#'     panel_widths = c(3, 5)
#'   )
#' }
#' }
```

Important notes:
- Use `n_samples = 20` with `warmup = 20, thin = 1` for minimal MCMC in examples (matches existing pool_methods.R pattern)
- Keep the `requireNamespace()` guards for Suggests dependencies (cards, gt, ggplot2, patchwork)
- All code inside `\donttest{}` (not `\dontrun{}`) since these CAN execute with ADMI data, they just take time
- Do NOT modify any other roxygen tags (@param, @return, @export, etc.) -- only replace the @examples block content
  </action>
  <verify>
- `grep -c "analyse_mi_data" R/ard_conversion.R` returns >= 1 (executable, not commented)
- `grep -c "analyse_mi_data" R/efficacy_table.R` returns >= 1
- `grep -c "analyse_mi_data" R/plot_forest.R` returns >= 1
- `grep -c "# pool_obj" R/ard_conversion.R` returns 0 (no more commented-out pseudocode)
- `grep -c "# pool_obj" R/efficacy_table.R` returns 0
- `grep -c "# pool_obj" R/plot_forest.R` returns 0
- Run `Rscript -e 'roxygen2::roxygenise()'` to verify roxygen parses without errors
  </verify>
  <done>pool_to_ard(), efficacy_table(), and plot_forest() have executable \donttest{} examples using ADMI data with full pipeline (analyse_mi_data -> pool -> function call)</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade describe_draws and describe_imputation examples to realistic ADEFF code</name>
  <files>R/describe.R</files>
  <action>
Replace the minimal @examples blocks for describe_draws(), print.describe_draws(), describe_imputation(), and print.describe_imputation() with realistic examples using ADEFF data.

IMPORTANT: describe_draws() and describe_imputation() operate on rbmi draws/impute objects (NOT on ADMI pre-imputed data). These functions need the full rbmi pipeline: ADEFF -> factor prep -> vars -> method -> draws() -> (optionally impute()). The draws() call here takes (data, vars, method) with NO data_ice argument since ADEFF does not have ICE columns and we are doing a simple jump-to-reference. Read R/describe.R to confirm: `describe_draws(draws_obj)` takes a draws object, `describe_imputation(impute_obj)` takes an imputation object.

**describe_draws() example** (replace the current @examples block, lines ~41-52):
```r
#' @examples
#' \dontrun{
#' library(rbmi)
#' library(dplyr)
#' data("ADEFF", package = "rbmiUtils")
#'
#' # Prepare ADEFF data for rbmi pipeline
#' ADEFF <- ADEFF |>
#'   mutate(
#'     TRT = factor(TRT01P, levels = c("Placebo", "Drug A")),
#'     USUBJID = factor(USUBJID),
#'     AVISIT = factor(AVISIT, levels = c("Week 24", "Week 48"))
#'   )
#'
#' vars <- set_vars(
#'   subjid = "USUBJID", visit = "AVISIT", group = "TRT",
#'   outcome = "CHG", covariates = c("BASE", "STRATA", "REGION")
#' )
#' dat <- ADEFF |> select(USUBJID, STRATA, REGION, TRT, BASE, CHG, AVISIT)
#' draws_obj <- draws(
#'   data = dat, vars = vars,
#'   method = method_bayes(n_samples = 100)
#' )
#'
#' # Inspect the draws object
#' desc <- describe_draws(draws_obj)
#' print(desc)
#'
#' # Programmatic access to metadata
#' desc$method
#' desc$n_samples
#' desc$formula
#' }
```

**print.describe_draws() example** (replace lines ~153-157):
```r
#' @examples
#' \dontrun{
#' # After creating draws_obj via the rbmi pipeline (see describe_draws):
#' desc <- describe_draws(draws_obj)
#' print(desc)  # Formatted cli output with method, formula, samples, convergence
#' }
```

**describe_imputation() example** (replace lines ~250-261):
```r
#' @examples
#' \dontrun{
#' library(rbmi)
#' library(dplyr)
#' data("ADEFF", package = "rbmiUtils")
#'
#' ADEFF <- ADEFF |>
#'   mutate(
#'     TRT = factor(TRT01P, levels = c("Placebo", "Drug A")),
#'     USUBJID = factor(USUBJID),
#'     AVISIT = factor(AVISIT, levels = c("Week 24", "Week 48"))
#'   )
#'
#' vars <- set_vars(
#'   subjid = "USUBJID", visit = "AVISIT", group = "TRT",
#'   outcome = "CHG", covariates = c("BASE", "STRATA", "REGION")
#' )
#' dat <- ADEFF |> select(USUBJID, STRATA, REGION, TRT, BASE, CHG, AVISIT)
#' draws_obj <- draws(
#'   data = dat, vars = vars,
#'   method = method_bayes(n_samples = 100)
#' )
#' impute_obj <- impute(
#'   draws_obj,
#'   references = c("Placebo" = "Placebo", "Drug A" = "Placebo")
#' )
#'
#' # Inspect the imputation
#' desc <- describe_imputation(impute_obj)
#' print(desc)
#'
#' # Programmatic access
#' desc$n_imputations
#' desc$missingness
#' desc$references
#' }
```

**print.describe_imputation() example** (replace lines ~347-351):
```r
#' @examples
#' \dontrun{
#' # After creating impute_obj via the rbmi pipeline (see describe_imputation):
#' desc <- describe_imputation(impute_obj)
#' print(desc)  # Formatted cli output with method, M, subjects, references, missingness
#' }
```

Important notes:
- describe_* examples MUST use `\dontrun{}` (not `\donttest{}`) because they require rbmi draws/impute objects that need MCMC computation which is slow
- Use ADEFF (not ADMI) since these functions operate on draws/impute objects, not pre-imputed data
- Show the full pipeline from data prep through to describe_* call so users understand the workflow
- print.describe_draws() and print.describe_imputation() use short examples that reference the parent function example rather than repeating undefined variables
- Do NOT modify any other roxygen tags -- only replace @examples content
  </action>
  <verify>
- `grep -c "ADEFF" R/describe.R` returns >= 2 (at least in describe_draws and describe_imputation examples)
- `grep -c "describe_draws(draws_obj)" R/describe.R` returns >= 1
- `grep -c "describe_imputation(impute_obj)" R/describe.R` returns >= 1
- `grep -c "desc\\$method" R/describe.R` returns >= 1
- No reference to undefined `data` variable in print.describe_draws example (the old print example used `draws(data, data_ice, vars, ...)` with undefined `data`)
- Run `Rscript -e 'roxygen2::roxygenise()'` to verify roxygen parses without errors
  </verify>
  <done>describe_draws() and describe_imputation() have realistic \dontrun{} examples using ADEFF data showing the full pipeline from data prep through to describe_* call with programmatic access demonstrations; print.* examples use short self-contained references without undefined variables</done>
</task>

</tasks>

<verification>
- All five key functions (pool_to_ard, efficacy_table, plot_forest, describe_draws, describe_imputation) have upgraded examples
- No commented-out pseudocode remains in the updated functions
- roxygen2::roxygenise() runs without errors
- \donttest{} used for ADMI-based examples (can execute), \dontrun{} used for ADEFF/MCMC examples (cannot execute cheaply)
- No undefined variables in any example (especially print.* examples)
</verification>

<success_criteria>
- Exported function examples use ADMI/ADEFF sample datasets showing real clinical analysis patterns
- No more commented-out pseudocode in pool_to_ard, efficacy_table, or plot_forest examples
- describe_draws and describe_imputation examples show realistic ADEFF pipeline usage
- roxygen2 processes all updated files without errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-documentation-overhaul/11-02-SUMMARY.md`
</output>
