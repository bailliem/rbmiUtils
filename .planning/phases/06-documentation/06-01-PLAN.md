---
phase: 06-documentation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - data-raw/generate-doc-images.R
  - man/figures/README-efficacy-table-1.png
  - man/figures/README-forest-plot-1.png
  - man/figures/plot_forest-trt.png
  - man/figures/efficacy_table-example.png
  - README.Rmd
  - README.md
  - R/plot_forest.R
  - R/efficacy_table.R
autonomous: true

must_haves:
  truths:
    - "README on GitHub shows a rendered efficacy table image and forest plot image as visual teasers"
    - "README links to BOTH the end-to-end pipeline vignette AND the analyse2 vignette"
    - "plot_forest() help page displays a rendered example output image"
    - "efficacy_table() help page displays a rendered example output image"
  artifacts:
    - path: "data-raw/generate-doc-images.R"
      provides: "Script to regenerate all documentation images from ADMI data"
      contains: "gtsave|ggsave"
    - path: "man/figures/README-efficacy-table-1.png"
      provides: "Efficacy table screenshot for README"
    - path: "man/figures/README-forest-plot-1.png"
      provides: "Forest plot image for README"
    - path: "man/figures/plot_forest-trt.png"
      provides: "Forest plot image for help page"
    - path: "man/figures/efficacy_table-example.png"
      provides: "Efficacy table image for help page"
    - path: "README.Rmd"
      provides: "Source README with visual teasers and links to both vignettes"
      contains: "man/figures/README-efficacy-table"
    - path: "README.md"
      provides: "Rendered README matching README.Rmd"
  key_links:
    - from: "README.Rmd"
      to: "man/figures/README-efficacy-table-1.png"
      via: "markdown image inclusion"
      pattern: "man/figures/README-efficacy-table"
    - from: "README.Rmd"
      to: "vignettes/pipeline.Rmd"
      via: "markdown hyperlink to pipeline vignette"
      pattern: "pipeline"
    - from: "README.Rmd"
      to: "vignettes/analyse2.Rmd"
      via: "markdown hyperlink to analyse2 vignette"
      pattern: "analyse2"
    - from: "R/plot_forest.R"
      to: "man/figures/plot_forest-trt.png"
      via: "roxygen2 figure tag"
      pattern: "figure.*plot_forest-trt"
    - from: "R/efficacy_table.R"
      to: "man/figures/efficacy_table-example.png"
      via: "roxygen2 figure tag"
      pattern: "figure.*efficacy_table-example"
---

<objective>
Generate documentation images, enhance README with visual teasers, and add rendered example output to plot_forest() and efficacy_table() help pages.

Purpose: Users see what rbmiUtils produces (forest plots, efficacy tables) immediately when visiting the GitHub page or reading function help -- visual proof of value before committing to learn the package.
Output: Image generation script, 4 PNG images in man/figures/, updated README.Rmd/README.md, updated roxygen in plot_forest.R and efficacy_table.R.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-RESEARCH.md

@R/plot_forest.R
@R/efficacy_table.R
@README.Rmd
@data-raw/DATASET.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create image generation script and produce all documentation images</name>
  <files>data-raw/generate-doc-images.R, man/figures/README-efficacy-table-1.png, man/figures/README-forest-plot-1.png, man/figures/plot_forest-trt.png, man/figures/efficacy_table-example.png</files>
  <action>
Create `data-raw/generate-doc-images.R` -- a standalone R script that generates all documentation images from the ADMI dataset. This script is a maintenance tool (run manually when outputs change), not part of the build process.

The script must:
1. Load rbmiUtils, rbmi, dplyr, ggplot2, patchwork, gt
2. Load and prepare ADMI data (same preprocessing as current README.Rmd: factor TRT with levels c("Placebo", "Drug A"), factor USUBJID, factor AVISIT)
3. Run the analysis pipeline: set_vars() -> method_bayes(n_samples=100, warmup=200, thin=5) -> analyse_mi_data() -> pool()
4. Generate 4 images:
   - `man/figures/README-forest-plot-1.png`: plot_forest(pool_obj, title = "Treatment Effect: Change from Baseline", arm_labels = c(ref = "Placebo", alt = "Drug A")) -- save with ggsave(), width=10, height=4.5, dpi=150
   - `man/figures/README-efficacy-table-1.png`: efficacy_table(pool_obj, title = "Table 14.2.1: ANCOVA of Change from Baseline", subtitle = "Mixed Model for Repeated Measures", arm_labels = c(ref = "Placebo", alt = "Drug A")) -- save with gt::gtsave(), vwidth=800
   - `man/figures/plot_forest-trt.png`: Same forest plot as README version (can reuse the object) -- save with ggsave(), width=10, height=4.5, dpi=150
   - `man/figures/efficacy_table-example.png`: Same table as README version (can reuse) -- save with gt::gtsave(), vwidth=800

Add a comment header explaining purpose and usage: "# Generate documentation images for README and help pages. Run: source('data-raw/generate-doc-images.R')"

After creating the script, run it in R to generate the actual PNG files:
```
cd /Users/bailliem/R-projects/rbmiUtils-gsd && Rscript data-raw/generate-doc-images.R
```

If webshot2 is not installed (needed by gt::gtsave for PNG), install it first. Alternative: use gt::gtsave() with .html extension and then convert, but webshot2 + chromote is the standard path.

Verify all 4 PNG files exist in man/figures/ after running.
  </action>
  <verify>
Run `ls -la man/figures/*.png` and confirm all 5 PNG files exist (rbmiUtils.png + 4 new ones). Check file sizes are reasonable (50-200KB each). Run `Rscript -e "file.exists('man/figures/README-forest-plot-1.png', 'man/figures/README-efficacy-table-1.png', 'man/figures/plot_forest-trt.png', 'man/figures/efficacy_table-example.png')"` -- all should return TRUE.
  </verify>
  <done>data-raw/generate-doc-images.R exists and is runnable. All 4 PNG images exist in man/figures/ with reasonable file sizes.</done>
</task>

<task type="auto">
  <name>Task 2: Update README.Rmd with visual teasers and rebuild, add rendered examples to roxygen</name>
  <files>README.Rmd, README.md, R/plot_forest.R, R/efficacy_table.R</files>
  <action>
**Part A: Rewrite README.Rmd** to be a concise visual teaser that drives users to the vignette.

Structure the new README.Rmd as:
1. Keep the existing YAML header (`output: github_document`), knitr opts_chunk setup (with `fig.path = "man/figures/README-"`), hex logo, badges
2. Opening paragraph: rbmiUtils bridges rbmi analysis results into publication-ready regulatory tables and forest plots. Mention it extends rbmi for clinical trial workflows.
3. **Installation** section: Keep the existing table (CRAN + GitHub)
4. **What You Get** section (replaces verbose Example + Utilities sections): Show the forest plot and efficacy table as static images with brief captions. Use `![Forest Plot](man/figures/README-forest-plot-1.png)` and `![Efficacy Table](man/figures/README-efficacy-table-1.png)`. These are the pre-generated images, NOT evaluated code chunks. Add a sentence: "These outputs are generated from a single rbmi pool object -- see the [end-to-end pipeline vignette](https://openpharma.github.io/rbmiUtils/articles/pipeline.html) for the complete walkthrough."
5. **Quick Start** section: A compact code snippet (using ADMI data, no draws() call) showing: load data -> set_vars -> method_bayes -> analyse_mi_data -> pool -> tidy_pool_obj -> efficacy_table / plot_forest. Use `eval = FALSE` for code chunks (no live execution of the slow pipeline in README rendering). Keep it under 30 lines of code.
6. **Key Features** section: Brief bullet list of capabilities (data validation, analysis execution, tidying, formatting, efficacy tables, forest plots, ARD conversion). Each bullet is one line with the function name in backticks.
7. **Learn More** section: MUST include EXPLICIT links to BOTH vignettes (LOCKED decision: keep both existing analyse2 vignette and new end-to-end vignette, link between them). Write exactly these entries:
   - `[From rbmi Analysis to Regulatory Tables](https://openpharma.github.io/rbmiUtils/articles/pipeline.html)` -- end-to-end walkthrough from raw data to regulatory outputs
   - `[Storing and Analyzing Imputed Data](https://openpharma.github.io/rbmiUtils/articles/analyse2.html)` -- focused guide on analysis workflows
   - Link to the pkgdown site: `https://openpharma.github.io/rbmiUtils/`
8. Remove the verbose Table of Contents, Data Validation Example, Datasets, Utilities subsections, and Reporting example from the old README. IMPORTANT: When removing old sections, preserve ALL vignette cross-references. The "Learn More" section (step 7) is the canonical location for vignette links -- verify it contains links to both pipeline.Rmd AND analyse2.Rmd after removals. The README should be scannable in under 30 seconds.
9. Keep the Development Status section at the bottom.

IMPORTANT: Do NOT use evaluated R code chunks that run draws() or analyse_mi_data(). The README images are pre-generated. The quick-start code is `eval = FALSE`. This avoids the slow MCMC computation during `devtools::build_readme()`.

**Part B: Rebuild README.md** by running:
```
cd /Users/bailliem/R-projects/rbmiUtils-gsd && Rscript -e "devtools::build_readme()"
```
Verify README.md is updated and images render correctly (check that image paths in README.md point to man/figures/).

**Part C: Add rendered example images to roxygen**

In `R/plot_forest.R`, add to the @details section (after the existing content, before @seealso):
```r
#' **Example output (treatment difference mode):**
#'
#' \if{html}{\figure{plot_forest-trt.png}{options: width=80\%}}
```

In `R/efficacy_table.R`, add to the @details section (after the existing content, before @seealso):
```r
#' **Example output:**
#'
#' \if{html}{\figure{efficacy_table-example.png}{options: width=80\%}}
```

After editing roxygen, run `Rscript -e "devtools::document()"` to regenerate .Rd files.

Then run `Rscript -e "devtools::check(args = '--no-tests')"` to verify R CMD check still passes (no broken figure references, no NOTE about missing files).
  </action>
  <verify>
1. `README.md` contains image references to `man/figures/README-forest-plot-1.png` and `man/figures/README-efficacy-table-1.png`
2. `README.md` contains link to pipeline vignette (pattern: `pipeline`)
3. `README.md` contains link to analyse2 vignette (pattern: `analyse2`) -- per LOCKED decision to link between both vignettes
4. `man/plot_forest.Rd` contains `\figure{plot_forest-trt.png}`
5. `man/efficacy_table.Rd` contains `\figure{efficacy_table-example.png}`
6. `R CMD check` passes with no errors (warnings about missing vignette are OK since pipeline.Rmd doesn't exist yet)
  </verify>
  <done>README.Rmd shows visual teasers and links to BOTH vignettes (pipeline and analyse2). README.md is rebuilt and in sync. plot_forest() and efficacy_table() help pages include rendered example images. R CMD check passes.</done>
</task>

</tasks>

<verification>
- All 4 documentation images exist in man/figures/ and are reasonable size
- README.md on GitHub would show forest plot and efficacy table images
- README.md links to BOTH the end-to-end pipeline vignette AND the analyse2 vignette
- `?plot_forest` in R shows the rendered forest plot image (in HTML help)
- `?efficacy_table` in R shows the rendered efficacy table image (in HTML help)
- `R CMD check` passes (excluding missing pipeline.Rmd vignette)
- `data-raw/generate-doc-images.R` is a reproducible script for regenerating images
</verification>

<success_criteria>
- DOC-02: README shows rendered efficacy table and forest plot as visual teasers, links to both vignettes
- DOC-03: plot_forest() help page displays rendered example output
- DOC-04: efficacy_table() help page displays rendered example output
- Image generation is reproducible via data-raw/generate-doc-images.R
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-01-SUMMARY.md`
</output>
