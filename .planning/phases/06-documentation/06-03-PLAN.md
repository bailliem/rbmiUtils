---
phase: 06-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - NEWS.md
  - vignettes/analyse2.Rmd
  - vignettes/data-preparation.Rmd
  - vignettes/efficient-storage.Rmd
  - R/analyse_mi_data.R
  - R/data_helpers.R
  - R/tidiers.R
  - R/plot_forest.R
  - R/efficacy_table.R
  - R/ard_conversion.R
  - R/imputation_storage.R
autonomous: true

must_haves:
  truths:
    - "NEWS.md documents v1 (0.1.0) and v2 (0.2.0) changes with proper version numbers and grouped sub-sections"
    - "Existing vignettes contain inline hyperlinks to rbmi and beeca documentation where relevant"
    - "Key functions have @seealso sections linking to rbmi/beeca functions they wrap or depend on"
  artifacts:
    - path: "NEWS.md"
      provides: "Version-organized changelog following tidyverse conventions"
      contains: "rbmiUtils 0.2.0"
    - path: "vignettes/analyse2.Rmd"
      provides: "Enhanced vignette with rbmi/beeca cross-references"
      contains: "cran.r-project.org/package=rbmi"
    - path: "vignettes/data-preparation.Rmd"
      provides: "Enhanced vignette with rbmi cross-references"
      contains: "cran.r-project.org/package=rbmi"
    - path: "vignettes/efficient-storage.Rmd"
      provides: "Enhanced vignette with rbmi cross-references"
      contains: "cran.r-project.org/package=rbmi"
  key_links:
    - from: "NEWS.md"
      to: "pkgdown changelog"
      via: "tidyverse heading conventions parsed by pkgdown::build_news()"
      pattern: "# rbmiUtils 0\\.[12]\\.0"
    - from: "vignettes/analyse2.Rmd"
      to: "rbmi documentation"
      via: "inline markdown hyperlinks"
      pattern: "https://.*rbmi"
    - from: "R/analyse_mi_data.R"
      to: "rbmi::analyse"
      via: "@seealso roxygen tag"
      pattern: "rbmi::analyse|rbmi::pool"
---

<objective>
Restructure NEWS.md with proper version history, add inline cross-references to existing vignettes, and enhance @seealso sections in roxygen for functions that wrap rbmi/beeca.

Purpose: Users can find version history in NEWS.md, discover related rbmi/beeca documentation from within vignettes and help pages, and the pkgdown changelog renders correctly.
Output: Restructured NEWS.md, enhanced vignettes with inline links, enhanced roxygen @seealso sections.
</objective>

<execution_context>
@/Users/bailliem/.claude/get-shit-done/workflows/execute-plan.md
@/Users/bailliem/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-documentation/06-CONTEXT.md
@.planning/phases/06-documentation/06-RESEARCH.md

@NEWS.md
@vignettes/analyse2.Rmd
@vignettes/data-preparation.Rmd
@vignettes/efficient-storage.Rmd
@R/analyse_mi_data.R
@R/data_helpers.R
@R/tidiers.R
@R/ard_conversion.R
@R/imputation_storage.R
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure NEWS.md into versioned changelog</name>
  <files>NEWS.md</files>
  <action>
Rewrite NEWS.md following tidyverse conventions (LOCKED decisions: organize by version number, v1=0.1.0, v2=0.2.0, group entries with sub-bullets).

The existing NEWS.md has:
- A "development version" section with v1/v2 features mixed together
- Old entries for 0.1.4 through 0.1.8 (mostly empty)

Restructure into this format:

```markdown
# rbmiUtils 0.2.0

## New Features

* End-to-end pipeline vignette "From rbmi Analysis to Regulatory Tables" with
  continuous and binary analysis walkthroughs.
* README enhanced with rendered efficacy table and forest plot visual teasers.
* Rendered example output images in `plot_forest()` and `efficacy_table()` help pages.
* Inline cross-references to rbmi and beeca documentation in all vignettes.

## Improvements

* `validate_data()` now uses cli-formatted error messages with clearer guidance
  for malformed interaction terms, empty data, and type mismatches.
* `prepare_data_ice()` now errors immediately when `vars$strategy` is NULL
  instead of silently using a default column name.
* `prepare_data_ice()` warns when visit column is character with guidance to
  convert to factor for correct ordering.
* `validate_data()` batches all type coercion warnings into a single message
  and warns on all-NA covariate columns.
* Data preparation functions handle edge cases (single subject, single visit,
  all-NA outcome, all-complete data) gracefully.

# rbmiUtils 0.1.0

## Breaking Changes

* `tidy_pool_obj()` now uses regex-based parameter parsing instead of splitting
  on `_`. Output columns (`parameter_type`, `lsm_type`, `visit`) now contain
  the full visit name rather than truncated fragments.

## New Features

* `efficacy_table()` creates regulatory-style gt summary tables from pool objects.
* `plot_forest()` creates publication-quality three-panel forest plots from pool objects.
* `pool_to_ard()` converts pool objects to pharmaverse ARD format via the cards package.
* `print()` and `summary()` S3 methods for pool and analysis objects.
* `create_impid()` converts lists of imputed datasets into stacked data.frames.
* `combine_results()` combines tidy results from multiple analyses.
* `format_results()` and `format_results_table()` for publication-ready formatting.
* `extract_trt_effects()` and `extract_lsm()` convenience filters for tidy results.

## Dependencies

* Added `cli` (>= 3.6.0) and `lifecycle` (>= 1.0.4) to Imports for improved
  error messaging and deprecation support.

## Improvements

* Enhanced input validation in `analyse_mi_data()` with clearer error messages
  using `inherits()` instead of internal class helpers.
* Enhanced input validation in `prepare_data_ice()` to check vars fields.
* Cross-references between related functions in documentation.
* Expanded test coverage and integration tests.

## Previous Releases

* `reduce_imputed_data()` and `expand_imputed_data()` for efficient storage
  of imputed datasets.
* `validate_data()` for pre-flight validation before `rbmi::draws()`.
* `prepare_data_ice()` to build `data_ice` from flagged ICE columns.
* `summarise_missingness()` for tabulating missing data patterns.
* `format_pvalue()`, `format_estimate()`, `format_results_table()` formatting utilities.
* `ADEFF` and `ADMI` example datasets.
* Initial documentation via pkgdown.
```

Notes:
- The 0.2.0 section includes Phase 5 (data prep hardening) and Phase 6 (documentation) changes. Phase 6 items are listed even though they are being implemented now -- this is standard practice since NEWS.md documents the upcoming release.
- The old 0.1.4-0.1.8 entries are consolidated into the 0.1.0 "Previous Releases" section since they were pre-release development versions.
- Heading format: `# rbmiUtils 0.x.0` (level 1), `## Section` (level 2) -- this is what pkgdown::build_news() parses.
  </action>
  <verify>
1. NEWS.md starts with `# rbmiUtils 0.2.0`
2. NEWS.md contains `# rbmiUtils 0.1.0`
3. No old 0.1.4-0.1.8 standalone sections remain
4. Run `Rscript -e "pkgdown::build_news()"` (if pkgdown is available) to verify it parses correctly -- or at minimum verify the heading structure matches `# package version` / `## Section` pattern
  </verify>
  <done>NEWS.md is restructured with 0.2.0 and 0.1.0 version sections, grouped sub-bullets (New Features, Improvements, Breaking Changes, Dependencies), and old pre-release entries consolidated.</done>
</task>

<task type="auto">
  <name>Task 2: Add cross-references to existing vignettes and @seealso to roxygen</name>
  <files>vignettes/analyse2.Rmd, vignettes/data-preparation.Rmd, vignettes/efficient-storage.Rmd, R/analyse_mi_data.R, R/data_helpers.R, R/tidiers.R, R/plot_forest.R, R/efficacy_table.R, R/ard_conversion.R, R/imputation_storage.R</files>
  <action>
**Part A: Add inline cross-references to existing vignettes** (LOCKED decision: inline hyperlinks woven into prose, not callout boxes)

Read each existing vignette and add inline hyperlinks where rbmi or beeca concepts are mentioned. Target: 3-6 links per vignette where they add value.

**vignettes/analyse2.Rmd** -- this vignette demonstrates analysis with rbmi+rbmiUtils:
- Where rbmi is first mentioned: add link to [rbmi](https://cran.r-project.org/package=rbmi)
- Where draws/impute/analyse/pool are mentioned: link to [rbmi quickstart vignette](https://cran.r-project.org/web/packages/rbmi/vignettes/quickstart.html) on first mention
- Where beeca is used (binary responder section): link to [beeca](https://openpharma.github.io/beeca/) and [beeca::get_marginal_effect()](https://openpharma.github.io/beeca/reference/get_marginal_effect.html)
- Where set_vars() is used: link to rbmi::set_vars in context
- Add a "See Also" paragraph at the end linking to the new pipeline vignette: "For a guided tutorial walking through the complete pipeline from raw data to regulatory tables, see `vignette('pipeline')`."

**vignettes/data-preparation.Rmd** -- this vignette covers data prep functions:
- Where rbmi::draws() is referenced: link to [rbmi](https://cran.r-project.org/package=rbmi) and the quickstart vignette
- Where ICE concepts are discussed: link to rbmi documentation on intercurrent events if available, or the rbmi CRAN page
- Add brief mention at end: "After data preparation, see `vignette('pipeline')` for the complete analysis workflow."

**vignettes/efficient-storage.Rmd** -- this vignette covers reduce/expand imputed data:
- Where imputed datasets from rbmi are discussed: link to [rbmi](https://cran.r-project.org/package=rbmi)
- Where the analysis workflow is mentioned: link to the pipeline vignette
- Keep additions minimal since this vignette is more self-contained

**Part B: Enhance @seealso sections in roxygen** (LOCKED decision: add @seealso sections for functions that wrap or depend on rbmi/beeca)

For each function below, add or enhance the @seealso section with links to the rbmi/beeca functions they wrap. Use the roxygen2 link syntax `[rbmi::function()]` for cross-package links and plain URLs for vignettes.

**R/analyse_mi_data.R** -- wraps rbmi::analyse():
Add to @seealso:
```r
#' * [rbmi::analyse()] which this function wraps
#' * [rbmi::pool()] for pooling the analysis results
#' * The [rbmi quickstart vignette](https://cran.r-project.org/web/packages/rbmi/vignettes/quickstart.html)
```

**R/data_helpers.R** (validate_data, prepare_data_ice, summarise_missingness):
- validate_data @seealso: add `[rbmi::draws()] which requires validated input data`
- prepare_data_ice @seealso: add reference to rbmi ICE handling
- summarise_missingness @seealso: add `[rbmi::draws()]` context

**R/tidiers.R** (tidy_pool_obj):
Add to @seealso: `[rbmi::pool()] which creates the pool objects this function tidies`

**R/plot_forest.R** -- already has @seealso but no rbmi links:
Add: `[rbmi::pool()] for creating pool objects`

**R/efficacy_table.R** -- already has @seealso but no rbmi links:
Add: `[rbmi::pool()] for creating pool objects`

**R/ard_conversion.R** (pool_to_ard) -- has @seealso to tidy_pool_obj and cards:
Add: `[rbmi::pool()] for creating pool objects`

**R/imputation_storage.R** (reduce_imputed_data, expand_imputed_data):
Add: `[rbmi::impute()] which creates the imputed datasets these functions operate on`

After all roxygen changes, run:
```
cd /Users/bailliem/R-projects/rbmiUtils-gsd && Rscript -e "devtools::document()"
```

Then verify with R CMD check:
```
Rscript -e "devtools::check(args = '--no-tests')"
```
  </action>
  <verify>
1. Each existing vignette contains at least 2 inline links to rbmi or beeca documentation
2. analyse2.Rmd links to the pipeline vignette
3. @seealso sections in key R files reference rbmi/beeca functions
4. `devtools::document()` runs without error
5. `R CMD check` passes (no broken cross-references, no new warnings)
  </verify>
  <done>All three existing vignettes contain inline hyperlinks to rbmi/beeca docs. Key roxygen @seealso sections link to the rbmi/beeca functions they wrap. R CMD check passes.</done>
</task>

</tasks>

<verification>
- NEWS.md follows tidyverse conventions with # rbmiUtils 0.2.0 and # rbmiUtils 0.1.0
- pkgdown can parse NEWS.md for changelog rendering
- All existing vignettes (analyse2, data-preparation, efficient-storage) have inline rbmi/beeca links
- analyse2.Rmd references the new pipeline vignette
- @seealso sections in analyse_mi_data, data_helpers, tidiers, plot_forest, efficacy_table, ard_conversion, imputation_storage link to rbmi/beeca
- R CMD check passes with no new errors or warnings
</verification>

<success_criteria>
- DOC-05: NEWS.md documents v1 (0.1.0) and v2 (0.2.0) changes with version numbers, grouped by New Features / Improvements / Breaking Changes
- DOC-06: Existing vignettes contain inline cross-references to rbmi and beeca documentation where relevant
- @seealso sections added to functions that wrap or depend on rbmi/beeca
</success_criteria>

<output>
After completion, create `.planning/phases/06-documentation/06-03-SUMMARY.md`
</output>
